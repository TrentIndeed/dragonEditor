import { MenuItem } from "../menu-item";

/**
 * Left content panel — always shows the active menu content
 * (Media, AI Pipeline, Text, etc.)
 *
 * Item properties are shown in the RightPanel instead.
 */
export const ControlItem = () => {
  return <MenuItem />;
};
